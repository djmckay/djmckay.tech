version: 1
applications:
  - appRoot: static-site
    frontend:
      phases:
        preBuild:
          commands:
            - npm ci
        build:
          commands:
            - npm run build
      artifacts:
        baseDirectory: _site
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
